# NFL Prediction Public Dashboard

Static public-facing version of the NFL matchup prediction dashboard.

The site reads precomputed JSON artifacts from `data/` and does not run Python or retrain models in production.

## Local Preview

Open `index.html` with a static server, for example:

```powershell
python -m http.server 8080
```

